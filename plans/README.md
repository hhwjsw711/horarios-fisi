# Implementation Plans

Generated by the improve skill on 2026-06-11 (advisor audit at commit `6fa5165`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Architecture docs + DB diagram | P1 | M | none | DONE (approved 2026-06-11 after 1 revision round for Spanish orthography; branch `advisor/001-architecture-docs`, 2 commits, pending merge by owner) |
| 003 | Quick hygiene (typecheck, webhook, admin script) | P1 | S | none | DONE (approved 2026-06-11; branch `advisor/003-quick-hygiene` at d90e5f4, pending merge by owner; shadcn step dropped, see rejected) |
| 002 | Transactional integrity + course-limit race | P1 | M | none | DONE (approved 2026-06-11; branch `advisor/002-transactional-integrity`, 4 commits, pending merge by owner) |
| 004 | Layer separation (domain types + lib folders) | P2 | M | 003 | DONE (approved 2026-06-11; branch `advisor/004-layer-separation` stacked on 003, 5 commits, pending merge by owner) |
| 005 | Characterization tests for schedule-db | P2 | L | 004, 002 | DONE (approved 2026-06-11; branch `advisor/005-schedule-db-tests` stacked on 004; 16 DB tests, skip-path verified; LIVE DB RUN PENDING: operator must create a Neon test branch and run `TEST_DATABASE_URL=... bun run test`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 requires 003 because `bun run typecheck` is the verification gate for the mechanical moves.
- 005 requires 004 (stable file paths) and 002 (it pins the post-fix atomicity contract: status change + audit event together).
- 001 and 003 are independent and can run in parallel branches.
- 002 touches only `src/lib/schedule-db.ts`; run it before 004 or rebase trivially after the move.

## Deferred findings (audited, planned for later, no plan yet)

- Split `schedule-app.tsx` (5,333 lines, 6 views): HIGH risk; blocked on 005 (tests) and 004 (types). Next candidate after this batch.
- Versioned migrations replacing `ensureScheduleSchema()`: blocked on 005.
- Timestamps stored as locale-formatted `text` (`submitted_at`, `approved_at`) instead of `timestamptz`: requires schema migration + backfill; bundle with the migrations work.
- `radix-ui` + `@base-ui/react` duplication: consolidate to Base UI; needs UI retesting.
- `xlsx@0.18.5` (abandoned community build): export-only exposure today; upgrade or swap when touching exports.
- Payload over-fetch (`getSchedulePayload` loads users/catalog/events for every role; events `limit 100` unpaginated): revisit with the schedule-app split.
- Timetable construction (`class_assignments` + conflict detection): direction finding, architecturally cheap (schema already has availability + courses per teacher); design spike when the product moves from collecting availability to building schedules.

## Findings considered and rejected

- "`shadcn` is an unused runtime dependency": FALSE. It is imported from CSS (`@import "shadcn/tailwind.css"` in `src/app/globals.css`); the audit grep only covered TS imports. Removing it breaks the build (verified by executor 2026-06-11, reverted). Keep the dependency.
- "Production secrets committed to the repo": FALSE. `.gitignore` covers `.env*`; git history contains only `.env.example` (verified with `git ls-files` + `git log --all -- ".env*"`). Live keys exist only as untracked local files, which is standard.
- "Missing `.catch()` freezes saving spinners in schedule-app": FALSE. `request()` (schedule-app.tsx:341) has internal try/catch/finally, toasts the error, and resolves `null`; the `.then()` chains always run.
- "`.env.example` missing DIRECTION_* vars": FALSE. Both `DIRECTION_ACCESS_CODE` and `DIRECTION_EMAIL_ALLOWLIST` are present.
- "Preview mode bypasses authorization": by design; non-production only (`NODE_ENV !== "production"` + `?preview=1`) and every mutation early-returns on `identity.preview`.
- Dev-route bypass in `src/proxy.ts` (NODE_ENV !== production): accepted as deliberate dev convenience; Vercel builds always set production. Revisit only if a non-Vercel deployment appears.
- Pre-commit hooks (husky): not adopted; `bun run check` + CI is the agreed gate, hooks add friction the maintainer didn't ask for.
